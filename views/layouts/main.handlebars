<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Attic</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Aboreto&display=swap">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="//code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>

    <nav class="navbar navbar-expand-lg">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">The Attic</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

      {{#if logged_in}}
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li><a class="nav-link" href="/profile">My Attic</a></li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">👤</a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/profile">My Posts</a></li>
                <li><a class="dropdown-item" href="/profile">Create Post</a></li>
                <li class="no-button" id="logout"><a class="dropdown-item" href="/">Log Out</a></li>
              </ul>
            </li>
          </ul>
        </div>
      {{else}}
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li><a class="nav-link" href="/login">Log In</a></li>
            <li class="nav-item">
              <a class="nav-link" href="/login">Sign Up</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">👤</a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/profile">My Posts</a></li>
                <li><a class="dropdown-item" href="/profile">Create Post</a></li>
              </ul>
            </li>
          </ul>
        </div>
      {{/if}}

      </div>
    </nav>

    <nav class="nav">
      <a class="nav-link" href="/">Home</a>
      <a class="nav-link" href="#">News</a>
      <a class="nav-link" href="#">Latest</a>
      <a class="nav-link" href="#">Reviews</a>
    </nav>
  </header>

  <main>
    <!-- Render the sub layout -->
    {{{ body }}}
  </main>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
